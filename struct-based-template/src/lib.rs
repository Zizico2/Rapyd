mod util;
/*

#[component]
mod Counter {
    struct Scope {
        #[prop]
        #[state]
        count: u32,
        
        #[prop(optional)]
        step: u32,
    }
    
    fn setup(scope: Scope) {}

    fn on_click(Scope { count, step }: Scope) {
        *count.borrow_mut() += 1;
    }

    html! {
        <button @click={on_click}>
            "Clicks: "{ count }
        </button>
    }
}

*/



